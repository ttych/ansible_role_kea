---

root: root
root_group: root

etc_dir: /etc
etc_local_dir: /etc

run_dir: /run
run_lock_dirname: lock
run_lock_dir: "/run/{{ run_lock_dirname }}"

var_app_dir: /var/lib

systemd_system_lib_dir: /lib/systemd/system

path_defaults:
  - /bin
  - /sbin
  - /usr/bin
  - /usr/sbin

path_package_base: /usr
